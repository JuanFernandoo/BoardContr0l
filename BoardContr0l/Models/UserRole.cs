namespace BoardContr0l.Models
{
    public enum UserRole // El enum permite manejar el rol de una forma mas estructurada
    {
        // Se le asigna un valor a cada rol
        Admin = 1,
        User = 2
    }
}
